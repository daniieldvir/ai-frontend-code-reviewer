import { Routes } from '@angular/router';
import { AnalyzerPage } from './pages/analyzer-page/analyzer-page';

export const routes: Routes = [
    {
      path: '',
      component: AnalyzerPage
    }
  ];